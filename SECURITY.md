# Security

Do not commit secrets or credentials in this repository. Use `.env.example` to document required variables.

If any secret was accidentally committed, rotate it immediately and add a short note to the release/PR explaining the rotation.
